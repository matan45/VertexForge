#pragma once

namespace types
{
    // Placeholder for future rendering settings
    // Examples: shadow quality, ambient occlusion, bloom, tone mapping, etc.
    struct RenderSettings
    {
        // Future settings will be added here

        static RenderSettings createDefault()
        {
            return RenderSettings{};
        }
    };
}
