#pragma once
// Terrain material nodes removed - terrain materials now use direct layer properties
// instead of a node-based graph. GLSL generation is handled by
// ShaderGraphCompiler::compileTerrainMaterial().
